import { Length3ValidatorDirective } from './length-3-validator.directive';

describe('Length3ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Length3ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
