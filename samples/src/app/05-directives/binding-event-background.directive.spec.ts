import { BindingEventBackgroundDirective } from './binding-event-background.directive';

describe('BindingEventBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingEventBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
