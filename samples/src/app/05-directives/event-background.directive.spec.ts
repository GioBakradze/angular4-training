import { EventBackgroundDirective } from './event-background.directive';

describe('EventBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
