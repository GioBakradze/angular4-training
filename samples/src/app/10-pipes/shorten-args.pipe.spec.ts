import { ShortenArgsPipe } from './shorten-args.pipe';

describe('ShortenArgsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenArgsPipe();
    expect(pipe).toBeTruthy();
  });
});
